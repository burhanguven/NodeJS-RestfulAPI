"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var fs_1 = require("fs");
function merge(a, b) {
    if (a && b) {
        for (var key in b) {
            a[key] = b[key];
        }
    }
    return a;
}
exports.merge = merge;
function compact(arr) {
    var narr = [];
    arr.forEach(function (data) {
        if (data) {
            narr.push(data);
        }
    });
    return narr;
}
exports.compact = compact;
function isModified(tsname, jsname) {
    var tsMTime = fs_1.statSync(tsname).mtime;
    var jsMTime = new Date(1984, 1, 17);
    try {
        jsMTime = fs_1.statSync(jsname).mtime;
    }
    catch (e) { //catch if file does not exists
    }
    return tsMTime > jsMTime;
}
exports.isModified = isModified;
